name: Docker

on:
  workflow_call: 

jobs:
  docker:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v6

    - name: Set up Docker build
      uses: docker/setup-buildx-action@v3

    - name: Docker Login
      uses: docker/login-action@v4.0.0
      with:
        username: ahshigueoka
        password: ${{ secrets.PASSWORD_DOCKER_HUB }}
              
